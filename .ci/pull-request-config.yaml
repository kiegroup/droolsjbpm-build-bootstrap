version: "2.1"

dependencies: ./project-dependencies.yaml

default:
  build-command:
    current: mvn -e -nsu -Dfull -Pwildfly clean install -Dcontainer.profile=wildfly -Dcontainer=wildfly -Dintegration-tests=false -DskipTests dependency:tree -Dmaven.wagon.httpconnectionManager.ttlSeconds=25 -Dmaven.wagon.http.retryHandler.count=3
